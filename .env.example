# PostgreSQL connection
# For local development with Docker: postgresql://postgres:postgres@localhost:5434/portfolio?schema=public
# For production: postgresql://user:password@host:5432/database?sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5434/portfolio?schema=public"

# Auth.js configuration
# Generate with: openssl rand -base64 32
AUTH_SECRET="your-auth-secret-here-generate-with-openssl-rand-base64-32"

# Admin credentials (single admin user)
# Email: The admin email for login
ADMIN_EMAIL="admin@example.com"
# Password hash: Generate with: npm run hash-password "your-secure-password"
ADMIN_PASSWORD_HASH="generate-with-npm-run-hash-password"

# Site URL for SEO metadata
# Required for generating absolute URLs for Open Graph images
# Set to your production domain (e.g., https://yourdomain.com)
NEXT_PUBLIC_SITE_URL="https://your-domain.com"
