---
phase: 05-public-portfolio
plan: 07
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04", "05-05", "05-06"]
files_modified:
  - src/app/globals.css
  - src/app/components/portfolio/glass-card.tsx
  - src/app/components/portfolio/section-divider.tsx
  - src/app/components/portfolio/project-card.tsx
  - src/app/components/portfolio/skill-card.tsx
  - src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Cards display strong glassmorphism effect (frosted glass)"
    - "Grain texture visible on glass cards"
    - "Subtle gradient lines appear between sections"
    - "Visual polish is consistent across all sections"
    - "ISR caching works correctly"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Glassmorphism and grain texture CSS"
      contains: "backdrop-filter"
    - path: "src/app/components/portfolio/glass-card.tsx"
      provides: "Reusable glass card wrapper"
      exports: ["GlassCard"]
    - path: "src/app/components/portfolio/section-divider.tsx"
      provides: "Gradient line between sections"
      exports: ["SectionDivider"]
  key_links:
    - from: "src/app/components/portfolio/glass-card.tsx"
      to: "src/app/globals.css"
      via: "glass utility classes"
      pattern: "glass-card"
    - from: "src/app/page.tsx"
      to: "src/app/components/portfolio/section-divider.tsx"
      via: "between sections"
      pattern: "SectionDivider"
---

<objective>
Apply visual polish with glassmorphism, grain texture, and gradient dividers throughout the portfolio.

Purpose: Elevates the visual quality to match the sophisticated design vision - bold frosted glass effects, subtle texture, and elegant transitions between sections.

Output: Polished portfolio with consistent glassmorphism on cards, grain texture effects, and gradient section dividers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-public-portfolio/05-CONTEXT.md
@.planning/phases/05-public-portfolio/05-01-SUMMARY.md
@.planning/phases/05-public-portfolio/05-02-SUMMARY.md
@.planning/phases/05-public-portfolio/05-03-SUMMARY.md
@.planning/phases/05-public-portfolio/05-04-SUMMARY.md
@.planning/phases/05-public-portfolio/05-05-SUMMARY.md
@.planning/phases/05-public-portfolio/05-06-SUMMARY.md

@src/app/page.tsx
@src/app/globals.css
@src/app/components/portfolio/project-card.tsx
@src/app/components/portfolio/skill-card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glassmorphism and grain texture CSS utilities</name>
  <files>src/app/globals.css</files>
  <action>
Add custom CSS utilities for glassmorphism and grain texture effects.

**Glassmorphism utility:**
```css
/* Strong glassmorphism effect - frosted glass look */
.glass-card {
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

/* Stronger glass for emphasis */
.glass-card-strong {
  background: rgba(255, 255, 255, 0.08);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.15);
}
```

**Grain texture (applied to cards):**
```css
/* Grain texture overlay - applied as pseudo element */
.grain::before {
  content: "";
  position: absolute;
  inset: 0;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
  opacity: 0.03;
  pointer-events: none;
  border-radius: inherit;
}

/* Card needs relative positioning for grain */
.grain {
  position: relative;
  overflow: hidden;
}
```

**Alternative grain approach (if SVG is too heavy):**
```css
.grain::before {
  content: "";
  position: absolute;
  inset: 0;
  background: repeating-radial-gradient(
    circle at 50% 50%,
    transparent 0,
    rgba(255, 255, 255, 0.03) 1px,
    transparent 2px
  );
  pointer-events: none;
  border-radius: inherit;
}
```

**Gradient section divider:**
```css
/* Horizontal gradient line that fades at edges */
.section-divider {
  height: 1px;
  background: linear-gradient(
    90deg,
    transparent 0%,
    var(--color-primary) 20%,
    var(--color-primary) 80%,
    transparent 100%
  );
  opacity: 0.3;
}
```

**Add smooth scroll if not present:**
```css
html {
  scroll-behavior: smooth;
}
```
  </action>
  <verify>CSS classes defined without syntax errors</verify>
  <done>Glassmorphism and grain texture CSS utilities available</done>
</task>

<task type="auto">
  <name>Task 2: Create glass card wrapper and section divider components</name>
  <files>src/app/components/portfolio/glass-card.tsx, src/app/components/portfolio/section-divider.tsx</files>
  <action>
Create reusable components for visual effects.

**GlassCard component:**
```tsx
interface GlassCardProps {
  children: React.ReactNode;
  className?: string;
  strong?: boolean; // Use stronger glass effect
  withGrain?: boolean; // Add grain texture
}

export function GlassCard({
  children,
  className = "",
  strong = false,
  withGrain = true,
}: GlassCardProps) {
  return (
    <div
      className={`
        ${strong ? "glass-card-strong" : "glass-card"}
        ${withGrain ? "grain" : ""}
        rounded-xl
        ${className}
      `}
    >
      {children}
    </div>
  );
}
```

**SectionDivider component:**
```tsx
interface SectionDividerProps {
  className?: string;
}

export function SectionDivider({ className = "" }: SectionDividerProps) {
  return (
    <div className={`section-divider max-w-4xl mx-auto ${className}`} />
  );
}
```

The divider should be subtle - just a visual breath between sections.
  </action>
  <verify>Components render with correct styles applied</verify>
  <done>GlassCard and SectionDivider components created</done>
</task>

<task type="auto">
  <name>Task 3: Apply glass effects to existing cards and add section dividers</name>
  <files>src/app/components/portfolio/skill-card.tsx, src/app/components/portfolio/project-card.tsx, src/app/page.tsx</files>
  <action>
Update existing card components to use glassmorphism and add dividers between sections.

**Update skill-card.tsx:**
- Wrap card content with GlassCard component OR apply glass-card class directly
- Add grain class for texture
- The effect should be subtle since skill cards are small

```tsx
// Option A: Direct classes
<div className="glass-card grain rounded-lg px-4 py-3 ...">

// Option B: GlassCard wrapper
<GlassCard className="px-4 py-3">
  ...
</GlassCard>
```

**Update project-card.tsx:**
- Apply strong glass effect to the card container
- This is where the "bold/obvious" glass look should shine
- The image already provides backdrop for the blur effect
- Ensure grain texture is applied

```tsx
<div className="glass-card-strong grain rounded-2xl overflow-hidden ...">
```

Note: Project cards have images as backgrounds, so glassmorphism should apply to overlay/content area rather than whole card. Adjust based on current card structure.

**Update page.tsx:**
Add SectionDivider between each section (inside the <main> element):

```tsx
<SectionWrapper id="hero">
  ...
</SectionWrapper>

<SectionDivider />

<SectionWrapper id="about">
  ...
</SectionWrapper>

<SectionDivider />

<SectionWrapper id="skills">
  ...
</SectionWrapper>

<SectionDivider />

<SectionWrapper id="projects">
  ...
</SectionWrapper>

<SectionDivider />

<SectionWrapper id="contact">
  ...
</SectionWrapper>
```

Consider the visual flow - dividers should be subtle separators, not hard breaks.
  </action>
  <verify>Cards show glassmorphism effect, gradient lines visible between sections</verify>
  <done>Visual polish applied throughout portfolio</done>
</task>

</tasks>

<verification>
1. Run `npm run dev` and visit http://localhost:3000
2. Skill cards: subtle glass effect with grain texture visible
3. Project cards: stronger glass effect, grain visible on overlays
4. Scroll through page: gradient lines appear between each section
5. Gradient lines fade at edges (not hard start/end)
6. Glass effects have proper backdrop-blur (frosted look)
7. Test on different backgrounds - glass effect adapts
8. Verify ISR working: check response headers for cache-control
   - `curl -I http://localhost:3000` should show revalidate directives
9. Full page performance: no jank from glass effects
10. Mobile: effects still visible and performant
</verification>

<success_criteria>
- Glassmorphism is "bold/obvious" (per CONTEXT.md)
- Grain texture visible on cards
- Gradient dividers appear between all sections
- Dividers are subtle (fade at edges)
- Purple accent remains sparingly used
- Visual consistency across all sections
- No performance degradation from effects
- ISR configured and working
</success_criteria>

<checkpoint type="human-verify" gate="blocking">
  <what-built>Complete portfolio visual polish - glassmorphism, grain texture, section dividers, and ISR caching</what-built>
  <how-to-verify>
    1. Visit http://localhost:3000
    2. Scroll through entire portfolio:
       - Hero section with geometric shapes
       - About section with bio and image
       - Skills section with category groups
       - Projects bento grid with hover effects
       - Contact section with email copy and socials
    3. Verify glass effect on cards is visible and "bold"
    4. Verify grain texture is subtle but present on cards
    5. Verify gradient dividers between sections
    6. Test responsive design:
       - Mobile (375px): single column, nav dots hidden
       - Tablet (768px): adapted layouts
       - Desktop (1280px): full bento grid, nav dots visible
    7. Test interactions:
       - Nav dot clicking scrolls smoothly
       - Project card hover shows links
       - Email copy works
       - Social links open correctly
       - Resume downloads
    8. Check browser devtools Network: verify no hydration errors
  </how-to-verify>
  <resume-signal>Type "approved" if portfolio looks polished and functions correctly, or describe any issues</resume-signal>
</checkpoint>

<output>
After completion, create `.planning/phases/05-public-portfolio/05-07-SUMMARY.md`
</output>
